<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!-- Hardened-runtime entitlements for notarized builds. The only one AmpCade
     needs is microphone/audio-input: under the hardened runtime the OS blocks
     input devices unless the entitlement is present, and a guitar app that
     can't hear the interface is a paperweight. -->
<plist version="1.0">
<dict>
    <key>com.apple.security.device.audio-input</key>
    <true/>
</dict>
</plist>
